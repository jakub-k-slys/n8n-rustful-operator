apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: singles.n8n.slys.dev
spec:
  group: n8n.slys.dev
  names:
    categories: []
    kind: Single
    plural: singles
    shortNames:
    - n8n
    singular: single
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SingleSpec via `CustomResource`
        properties:
          spec:
            description: '`Single` is a Kubernetes-native description of a standalone n8n deployment.'
            properties:
              database:
                description: Database backend (sqlite default).
                nullable: true
                properties:
                  mysql:
                    nullable: true
                    properties:
                      connectionTimeoutMs:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      passwordSecret:
                        properties:
                          key:
                            default: encryption_key
                            description: Key within the Secret. Defaults to `encryption_key`.
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      port:
                        format: int32
                        nullable: true
                        type: integer
                      ssl:
                        nullable: true
                        properties:
                          caSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          certSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          enabled:
                            default: false
                            type: boolean
                          keySecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          rejectUnauthorized:
                            nullable: true
                            type: boolean
                        type: object
                      user:
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - user
                    type: object
                  postgres:
                    nullable: true
                    properties:
                      connectionTimeoutMs:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      passwordSecret:
                        properties:
                          key:
                            default: encryption_key
                            description: Key within the Secret. Defaults to `encryption_key`.
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      poolSize:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      port:
                        format: int32
                        nullable: true
                        type: integer
                      schema:
                        nullable: true
                        type: string
                      ssl:
                        nullable: true
                        properties:
                          caSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          certSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          enabled:
                            default: false
                            type: boolean
                          keySecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          rejectUnauthorized:
                            nullable: true
                            type: boolean
                        type: object
                      user:
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - user
                    type: object
                  sqlite:
                    nullable: true
                    properties:
                      database:
                        description: Path inside the pod, mapped to `DB_SQLITE_DATABASE`.
                        nullable: true
                        type: string
                      poolSize:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      vacuumOnStartup:
                        nullable: true
                        type: boolean
                    type: object
                  type:
                    default: sqlite
                    description: '`sqlite` (default), `postgresdb`, `mysqldb` or `mariadb` — maps to `DB_TYPE`.'
                    type: string
                type: object
              encryptionKey:
                description: N8N_ENCRYPTION_KEY source. Auto-generated if omitted.
                nullable: true
                properties:
                  secretRef:
                    description: Reference to an existing Secret. Omit the whole block to auto-generate.
                    nullable: true
                    properties:
                      key:
                        default: encryption_key
                        description: Key within the Secret. Defaults to `encryption_key`.
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              host:
                description: Externally-facing hostname. Required when `networking` is set.
                nullable: true
                type: string
              image:
                default: n8nio/n8n:latest
                description: Container image to deploy (e.g. `n8nio/n8n:1.70.0`).
                type: string
              networking:
                nullable: true
                properties:
                  httpRoute:
                    description: Provision a `gateway.networking.k8s.io/v1` HTTPRoute. Mutually exclusive with `ingress`.
                    nullable: true
                    properties:
                      gateway:
                        description: Gateway to attach this HTTPRoute to (parentRefs[0]).
                        properties:
                          name:
                            type: string
                          namespace:
                            nullable: true
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - gateway
                    type: object
                  ingress:
                    description: Provision a `networking.k8s.io/v1` Ingress. Mutually exclusive with `httpRoute`.
                    nullable: true
                    properties:
                      className:
                        nullable: true
                        type: string
                      tlsSecretName:
                        description: Name of a TLS Secret in the same namespace.
                        nullable: true
                        type: string
                    type: object
                type: object
              persistence:
                description: PVC at `/home/node/.n8n` so binary data and the sqlite file persist.
                nullable: true
                properties:
                  accessMode:
                    default: ReadWriteOnce
                    type: string
                  size:
                    description: Storage request, e.g. `1Gi`.
                    type: string
                  storageClassName:
                    nullable: true
                    type: string
                required:
                - size
                type: object
              replicas:
                default: 1
                format: int32
                type: integer
              service:
                nullable: true
                properties:
                  type:
                    default: ClusterIP
                    description: '`ClusterIP` (default), `NodePort`, or `LoadBalancer`.'
                    type: string
                type: object
            type: object
          status:
            nullable: true
            properties:
              encryptionKeySecret:
                nullable: true
                type: string
              ready:
                type: boolean
              replicas:
                format: int32
                type: integer
              url:
                nullable: true
                type: string
            required:
            - ready
            - replicas
            type: object
        required:
        - spec
        title: Single
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusters.n8n.slys.dev
spec:
  group: n8n.slys.dev
  names:
    categories: []
    kind: Cluster
    plural: clusters
    shortNames:
    - n8nc
    singular: cluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ClusterSpec via `CustomResource`
        properties:
          spec:
            properties:
              database:
                description: Queue mode requires a shared DB; sqlite is rejected.
                properties:
                  mysql:
                    nullable: true
                    properties:
                      connectionTimeoutMs:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      passwordSecret:
                        properties:
                          key:
                            default: encryption_key
                            description: Key within the Secret. Defaults to `encryption_key`.
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      port:
                        format: int32
                        nullable: true
                        type: integer
                      ssl:
                        nullable: true
                        properties:
                          caSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          certSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          enabled:
                            default: false
                            type: boolean
                          keySecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          rejectUnauthorized:
                            nullable: true
                            type: boolean
                        type: object
                      user:
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - user
                    type: object
                  postgres:
                    nullable: true
                    properties:
                      connectionTimeoutMs:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      passwordSecret:
                        properties:
                          key:
                            default: encryption_key
                            description: Key within the Secret. Defaults to `encryption_key`.
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      poolSize:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      port:
                        format: int32
                        nullable: true
                        type: integer
                      schema:
                        nullable: true
                        type: string
                      ssl:
                        nullable: true
                        properties:
                          caSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          certSecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          enabled:
                            default: false
                            type: boolean
                          keySecret:
                            nullable: true
                            properties:
                              key:
                                default: encryption_key
                                description: Key within the Secret. Defaults to `encryption_key`.
                                type: string
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          rejectUnauthorized:
                            nullable: true
                            type: boolean
                        type: object
                      user:
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - user
                    type: object
                  sqlite:
                    nullable: true
                    properties:
                      database:
                        description: Path inside the pod, mapped to `DB_SQLITE_DATABASE`.
                        nullable: true
                        type: string
                      poolSize:
                        format: uint32
                        minimum: 0.0
                        nullable: true
                        type: integer
                      vacuumOnStartup:
                        nullable: true
                        type: boolean
                    type: object
                  type:
                    default: sqlite
                    description: '`sqlite` (default), `postgresdb`, `mysqldb` or `mariadb` — maps to `DB_TYPE`.'
                    type: string
                type: object
              encryptionKey:
                nullable: true
                properties:
                  secretRef:
                    description: Reference to an existing Secret. Omit the whole block to auto-generate.
                    nullable: true
                    properties:
                      key:
                        default: encryption_key
                        description: Key within the Secret. Defaults to `encryption_key`.
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              image:
                default: n8nio/n8n:latest
                description: Cascading default image. Each role can override.
                type: string
              main:
                default:
                  replicas: 0
                properties:
                  host:
                    nullable: true
                    type: string
                  image:
                    nullable: true
                    type: string
                  networking:
                    nullable: true
                    properties:
                      httpRoute:
                        description: Provision a `gateway.networking.k8s.io/v1` HTTPRoute. Mutually exclusive with `ingress`.
                        nullable: true
                        properties:
                          gateway:
                            description: Gateway to attach this HTTPRoute to (parentRefs[0]).
                            properties:
                              name:
                                type: string
                              namespace:
                                nullable: true
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - gateway
                        type: object
                      ingress:
                        description: Provision a `networking.k8s.io/v1` Ingress. Mutually exclusive with `httpRoute`.
                        nullable: true
                        properties:
                          className:
                            nullable: true
                            type: string
                          tlsSecretName:
                            description: Name of a TLS Secret in the same namespace.
                            nullable: true
                            type: string
                        type: object
                    type: object
                  persistence:
                    description: PVC at `/home/node/.n8n` for the main pod only.
                    nullable: true
                    properties:
                      accessMode:
                        default: ReadWriteOnce
                        type: string
                      size:
                        description: Storage request, e.g. `1Gi`.
                        type: string
                      storageClassName:
                        nullable: true
                        type: string
                    required:
                    - size
                    type: object
                  replicas:
                    default: 1
                    format: int32
                    type: integer
                  service:
                    nullable: true
                    properties:
                      type:
                        default: ClusterIP
                        description: '`ClusterIP` (default), `NodePort`, or `LoadBalancer`.'
                        type: string
                    type: object
                type: object
              redis:
                properties:
                  db:
                    format: uint32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  host:
                    type: string
                  passwordSecret:
                    nullable: true
                    properties:
                      key:
                        default: encryption_key
                        description: Key within the Secret. Defaults to `encryption_key`.
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  port:
                    format: uint16
                    maximum: 65535.0
                    minimum: 0.0
                    nullable: true
                    type: integer
                  prefix:
                    description: '`QUEUE_BULL_PREFIX` for namespacing within a shared Redis instance.'
                    nullable: true
                    type: string
                  tls:
                    nullable: true
                    type: boolean
                  usernameSecret:
                    nullable: true
                    properties:
                      key:
                        default: encryption_key
                        description: Key within the Secret. Defaults to `encryption_key`.
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - host
                type: object
              webhooks:
                nullable: true
                properties:
                  host:
                    nullable: true
                    type: string
                  image:
                    nullable: true
                    type: string
                  networking:
                    nullable: true
                    properties:
                      httpRoute:
                        description: Provision a `gateway.networking.k8s.io/v1` HTTPRoute. Mutually exclusive with `ingress`.
                        nullable: true
                        properties:
                          gateway:
                            description: Gateway to attach this HTTPRoute to (parentRefs[0]).
                            properties:
                              name:
                                type: string
                              namespace:
                                nullable: true
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - gateway
                        type: object
                      ingress:
                        description: Provision a `networking.k8s.io/v1` Ingress. Mutually exclusive with `httpRoute`.
                        nullable: true
                        properties:
                          className:
                            nullable: true
                            type: string
                          tlsSecretName:
                            description: Name of a TLS Secret in the same namespace.
                            nullable: true
                            type: string
                        type: object
                    type: object
                  replicas:
                    format: int32
                    type: integer
                  service:
                    nullable: true
                    properties:
                      type:
                        default: ClusterIP
                        description: '`ClusterIP` (default), `NodePort`, or `LoadBalancer`.'
                        type: string
                    type: object
                required:
                - replicas
                type: object
              workers:
                properties:
                  autoscaling:
                    description: HPA opts the worker Deployment into horizontal autoscaling.
                    nullable: true
                    properties:
                      maxReplicas:
                        format: int32
                        type: integer
                      minReplicas:
                        format: int32
                        type: integer
                      targetCPUUtilizationPercentage:
                        format: int32
                        nullable: true
                        type: integer
                    required:
                    - maxReplicas
                    - minReplicas
                    type: object
                  concurrency:
                    description: Maps to `N8N_CONCURRENCY_PRODUCTION_LIMIT`.
                    format: uint32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  image:
                    nullable: true
                    type: string
                  replicas:
                    description: Static replica count. Ignored when `autoscaling` is set.
                    format: int32
                    type: integer
                required:
                - replicas
                type: object
            required:
            - database
            - redis
            - workers
            type: object
          status:
            nullable: true
            properties:
              encryptionKeySecret:
                nullable: true
                type: string
              mainReplicas:
                format: int32
                type: integer
              ready:
                type: boolean
              webhookReplicas:
                format: int32
                type: integer
              workerReplicas:
                format: int32
                type: integer
            required:
            - mainReplicas
            - ready
            - webhookReplicas
            - workerReplicas
            type: object
        required:
        - spec
        title: Cluster
        type: object
    served: true
    storage: true
    subresources:
      status: {}
